name: Linters

on:
  push:
    branches: [main, dev]
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  markdown-and-build:
    uses: finki-hub/.github/.github/workflows/docs-check.yaml@main
