# PostHog product analytics (PUBLIC, write-only browser ingest key).
# Vite exposes VITE_* to client code via import.meta.env.
# The theme falls back to this same public key when unset, so the build
# embeds analytics even without configuring this variable.
VITE_POSTHOG_KEY=phc_xXEqLMnYeDPuXA6HHwuasQMdSufDGryS8vZZuHmu9Qwd
VITE_POSTHOG_HOST=https://eu.i.posthog.com
